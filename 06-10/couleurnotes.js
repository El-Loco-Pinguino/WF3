var toutesLesNotes = document.getElementById("note")
var epsilon = 0

epsilon = toutesLesNotes.length
var alpha = 0
while (alpha < epsilon) { // alpha correspond à l'ID de tou
	if (epsilon[alpha] < 10) {
		this.addClass = "faible"
	} else if (epsilon[alpha] > 10) {
		this.addClass = "bien"
	} else this.addClass = "juste"
alpha++
}